name: spy_scanner
description: "Neon Apps - Spy Scanner - Final Project"
publish_to: 'none' 

version: 1.0.0+4

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Network Utils
  network_info_plus: ^7.0.0
  network_tools: ^6.0.5
  dart_ping: ^9.0.1
  dart_ping_ios: ^4.0.2
  flutter_nsd: ^1.6.0
  nsd: ^4.0.3

  # Bluetooth
  flutter_reactive_ble: ^5.4.0

  # Utils
  path_provider: ^2.1.5
  get_it: ^8.2.0
  logging: ^1.3.0
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0

flutter:
  uses-material-design: true
